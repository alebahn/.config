# Dotfiles

## Setup
1. Clone into .config

2. install
```
$ INSTALL
```

3. create a `.gitconfig` file with the folowing:
```
[user]
    name = Aaron Lebahn
    email = {email}
[include]
    path = .config/git/common.gitconfig
```

# Dotfiles

## Setup
1. Clone into .config

2. Install
    ```
    $ ./INSTALL
    ```

3. Create a `.gitconfig` file with the folowing:
    ```
    [user]
        name = Aaron Lebahn
        email = {email}
    ```

4. Optionally, merge existing .bashrc with new bashrc

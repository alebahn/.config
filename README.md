# Dotfiles

## Setup
Clone into .config

install
```
$ INSTALL
```

create a `.gitconfig` file with the folowing:
```
[user]
    name = Aaron Lebahn
    email = {email}
[include]
    path = .config/git/common.gitconfig
```
